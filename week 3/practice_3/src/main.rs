fn main() {
    let result = 10.00; // no type stated, so this is f64
    let interest: f32 = 8.35; // single precision, stated yourself
    let cost: f64 = 15000.600; // double precision, stated yourself

    println!("result: {}", result);
    println!("interest: {}", interest);
    println!("cost: {}", cost);
}
